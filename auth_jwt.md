# Authentification JWT - CarRent

## Implémentation
- Utilisation de JWT pour sécuriser les endpoints
- Stockage du token via Expo SecureStore
- Vérification automatique du token à chaque requête

## Endpoints protégés
- /api/reservations
- /api/profile
- /api/paiements
